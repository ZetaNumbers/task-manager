[package]
name = "task-manager"
version = "0.1.0"
edition = "2018"

[dependencies]
rocket = "0.4.7"
task-manager-types = { path = "types" }
rocket_contrib = { version = "0.4.7", features = ["serve"] }
serde_yaml = "0.8.17"
libc = "0.2.92"

[workspace]
members = ["types", "client"]
